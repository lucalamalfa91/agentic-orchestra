# Production requirements — torch installed separately via CPU-only index URL (see Dockerfile)

# Core web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-dotenv>=1.0.0
pydantic>=2.0.0

# Database
sqlalchemy>=2.0.0
alembic>=1.12.0
asyncpg>=0.29.0
pgvector>=0.2.0
psycopg2-binary>=2.9.0

# Auth & Security
cryptography>=41.0.0
PyJWT>=2.8.0

# LangGraph orchestration
langgraph>=0.2.0
langgraph-checkpoint-postgres>=0.0.1
langchain-core>=0.3.0
langchain-openai>=0.2.0
langchain-anthropic>=0.2.0

# Embeddings (torch installed separately with CPU-only wheels)
sentence-transformers>=2.2.0

# HTTP / Scraping
requests>=2.31.0
httpx>=0.25.0
beautifulsoup4>=4.12.0

# File parsing
pypdf>=3.17.0
python-docx>=1.1.0

# GitHub integration
PyGithub>=2.1.0

# MCP Protocol
mcp>=1.0.0
deepagents

# JSON utilities
json-repair>=0.3.0
